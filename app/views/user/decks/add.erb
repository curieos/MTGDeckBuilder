<div class="card-header">
	<h1>Searching for <%= params[:card_name] %></h1>
</div>
<div class="card-body">
	<div class="row">
		<% if !@cards.blank? %>
		<% for card in @cards %>
		<div class="col-sm-3 col-md-2 mb-2">
			<form action="/user/decks/<%= params[:id] %>/add" method="post">
				<input id="hidden" type="hidden" name="card_id" value="<%= card.id %>">
				<button type="submit" href="#" class="card">
					<img src="<%= card.image_uri %>" class="card-img-top" alt="<%= card.name %>">
					<div class="card-body">
						<%= card.name %>
					</div>
				</button>
			</form>
		</div>
		<% end %>
		<% end %>
	</div>
</div>